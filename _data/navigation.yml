# Single-page site with no top navigation bar (the masthead is hidden via CSS).
# To restore a top menu, unhide `.masthead` in assets/css/main.scss and add
# items below, e.g.:
#   - title: "Publications"
#     url: /#selected-publications

main:
